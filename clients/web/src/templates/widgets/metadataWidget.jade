include metadatumViewMixin
if (accessLevel >= girder.AccessType.WRITE)
  button.btn.btn-sm.btn-primary.g-widget-metadata-add-button(title="Add Metadata")
    i.icon-plus
.g-widget-metadata-header
  i.icon-tags
  |  Metadata
.g-widget-metadata-container
  each keyValue in meta
    .g-widget-metadata-row(g-key="#{keyValue.key}", g-value="#{keyValue.value}")
      +metadatum(keyValue.key, keyValue.value, keyValue.isJson, accessLevel, girder)
