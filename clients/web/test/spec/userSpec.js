/**
 * This is a stub jasmine spec that is currently used as a simple
 * proof of concept.
 */
describe('UserModel Testing', function () {
    it('is defined', function () {
        var user = new girder.models.UserModel();
        expect(user).toBeDefined();
    });
});
