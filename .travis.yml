language: cpp

services: mongodb

compiler:
  - clang

install:
  - "sudo pip install -r requirements.txt"
  - "sudo python setup.py install"
  - "sudo npm install -g grunt grunt-cli"
  - "npm install"
  - "grunt init"
  - "grunt"

before_script:
  - mkdir _build
  - cd _build

script:
  - cmake ..
  - make
  - ctest -V
