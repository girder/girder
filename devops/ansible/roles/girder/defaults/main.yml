---
# defaults file for girder

girder_path: "{{ ansible_user_dir }}/girder"
girder_version: "master"
girder_update: yes
girder_force: yes
girder_web: yes
girder_start: yes
girder_enabled: yes

# the default is to only build assets when the git step results in new files
girder_always_build_assets: no
