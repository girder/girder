version: '2'
services:
  mongodb:
    image: mongo:3.2
    volumes:
      - "/data/db"
  girder:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - "mongodb"
    command: --host 0.0.0.0 --database mongodb://mongodb:27017/girder
